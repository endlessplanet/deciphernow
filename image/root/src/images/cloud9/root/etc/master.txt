file_roots:
  base:
    - /srv/salt/base/states
  test:
    - /srv/salt/test/states
    - /srv/salt/base/states
pillar_roots:
  base:
      - /srv/pillar/base
  test:
      - /srv/pillar/test
      - /srv/pillar/base